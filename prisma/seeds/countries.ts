import { PrismaClient } from "@prisma/client";

export async function seedCountries(_prisma: PrismaClient) {
  // Seed placeholder architecture for country records (USA, UK, Canada, Australia, Germany, Ireland, Singapore)
}
